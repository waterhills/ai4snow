# Foundation

Shared visual foundation for AI4Snow: video → human detection → target tracking → pose estimation → temporal processing → unified human-motion data. Upper-level evaluation modules should consume this unified output.
